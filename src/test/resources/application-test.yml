spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false

  jpa:
    hibernate:
      ddl-auto: validate

  jwt:
    secret: "test-secret-key-which-is-at-least-32-chars-long!!!"
    expiration: 3600000